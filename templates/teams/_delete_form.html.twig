<form method="post" action="{{ path('app_teams_delete', {'id': team.id}) }}" onsubmit="return confirm('Êtes-vous certain de vouloir supprimer cette team ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ team.id) }}">
    <button class="w-10/12 bg-red-600 hover:bg-red-800 rounded-lg h-8 m-2 pl-2 pr-3 text-white text-12 text-center font-bold">Supprimer</button>
</form>
