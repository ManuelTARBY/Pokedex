<form method="post" action="{{ path('app_caught_delete', {'id': caught.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ caught.id) }}">
    <button class="btn">Delete</button>
</form>
