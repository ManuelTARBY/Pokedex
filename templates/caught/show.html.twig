{% extends 'base.html.twig' %}

{% block title %}Caught{% endblock %}

{% block body %}
    <h1>Caught</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ caught.id }}</td>
            </tr>
            <tr>
                <th>Date_capture</th>
                <td>{{ caught.dateCapture ? caught.dateCapture|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_caught_index') }}">back to list</a>

    <a href="{{ path('app_caught_edit', {'id': caught.id}) }}">edit</a>

    {{ include('caught/_delete_form.html.twig') }}
{% endblock %}
