<form method="post" action="{{ path('app_user_delete', {'id': user.id}) }}" onsubmit="return confirm('Êtes-vous certain de vouloir supprimer ce compte ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <div class="m-5">
        <button class="w-full text-xl text-white bg-red-600 hover:bg-red-800 rounded-lg  text-center">Supprimer le compte</button>
    </div>
</form>
    