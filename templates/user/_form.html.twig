{{ form_start(form) }}
    <div class="">{{ form_widget(form) }}</div>
    <div class="text-xl text-white bg-sky-400 hover:bg-sky-600 rounded-lg w-60 m-5 text-center">
        <button class="btn">{{ button_label|default('Save') }}</button>
    </div>
{{ form_end(form) }}
