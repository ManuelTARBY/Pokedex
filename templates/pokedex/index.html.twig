{% extends 'base.html.twig' %}

{% block title %}Hello PokedexController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <table>
        <tr>
            <th>Numéro</th>
            <th>Nom</th>
            <th>Type</th>
        </tr>
    {% for ligne in results %}
        {% set lestypes = ligne.logos %}
        {% set leslogos = lestypes | split(',') %}
        <tr>
            <th>{{ ligne.pokedex_id }}</th>
            <th>{{ ligne.name_fr }}</th>
            <th>
            {% for logo in leslogos %}
                <img src="{{ logo }}">
            {% endfor %}
            </th>  
        </tr>
    {% endfor %}
    </table>
</div>
{% endblock %}
